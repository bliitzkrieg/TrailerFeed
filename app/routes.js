(function(){
	'use strict';
 /*
	angular
		.module('app')
		.config(config);

	function config($routeProvider){
		$routeProvider
			.when('/', {
				controller: 'IndexController',
				controllerAs: 'vm',
				templateUrl: 'partials/main/home.html'
			})
			.otherwise( { 
				redirectTo: "/" 
			});
	}
 */
})();